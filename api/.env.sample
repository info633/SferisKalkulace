# --- SFERIS API (MVP) environment example ---
# CORS origin for the frontend (comma separate if multiple)
CORS_ORIGIN=http://localhost:5500
# Comma-separated list of admin emails
SFERIS_ADMINS=martin.fiser@sferis.cz
# Optional Make webhook for RAYNET export
MAKE_WEBHOOK_URL=https://hook.make.com/your-webhook-id
